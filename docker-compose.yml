version: "3.8"

services:
  jep-web:
    build:
      context: .
      dockerfile: docker/Dockerfile
    ports:
      - "8080:8080"
    volumes:
      - ./events:/app/events:ro
    environment:
      - JEP_STORAGE_PATH=/app/events
    command: jep web --host 0.0.0.0 --port 8080
